name: Test actions

on:
  workflow_dispatch:

jobs:
  say-hello-inline-bash:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Hello Inline World"
  say-hello-python:
    runs-on: ubuntu-24.04
    steps:
      - run: print("Hello Python World")
        shell: python
      
